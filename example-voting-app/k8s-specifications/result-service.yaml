apiVersion: v1
kind: Service
metadata:
  name: result
spec:
  type: NodePort
  ports:
    - name: "result-service"
      port: 80
      targetPort: 80
      nodePort: 30002
  selector:
    app: result
